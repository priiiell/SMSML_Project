global:
  scrape_interval: 5s # Menarik data metrik setiap 5 detik sekali

scrape_configs:
  - job_name: 'mlflow-model-exporter'
    static_configs:
      - targets: ['host.docker.internal:8000'] # Mengarah ke aplikasi pengekspor metrik Python kita nanti